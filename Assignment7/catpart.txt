# File
  Size:
      Empty.			[single][property emptyfile]			
      Not empty.
  Number of occurrences of the fields in the file:
      None.			[single][if !emptyfile] [property noOccurences]			
      One.			[if !emptyfile][property oneField]			
      Many.			[if !emptyfile]						
  Position of the fields in the file:
      First line.			[single][if !noOccurences && !emptyfile]		
      Last line.			[single][if !noOccurences && !emptyfile]		
      Any.			[if !noOccurences && !emptyfile]	
  Number of lines with all specified fields:
      None.			[single][if !noOccurences && !emptyfile] 			
      One.			[single][if !noOccurences && !emptyfile][property oneLine]	
      Many.  			[if !noOccurences && !emptyfile]
  Position of the field on the line:
      Beginning.			[single][if !noOccurences && !emptyfile && oneLine && oneField] 
      Middle.				[single][if !noOccurences && !emptyfile && oneLine && oneField] 
      End.			[single][if !noOccurences && !emptyfile && oneLine && oneField] 

# Fields	
  Number of fields:
      None.			[error]
      One single index.			[property singleIndex]
      One range.			[property singleRange]
      Multiple single indicies only.			[single][property MultipleIndiciesOnly]
      Multiple ranges only.			[single][property multipleRangesOnly]
      Multiple single indicies and ranges.			[property multipleRangeAndIndex]	
  Presence of separating comma:
      None.			[if singleIndex || singleRange]
      One or more.			[if !singleIndex && !singleRange]							
  Value of range start:
      Less than one.			[error]
      One or more.			[if singleIndex || singleRange] 
  Value of range end:
      Less than range start.			[error]
      Equal to range start.			[if singleIndex || singleRange]
      Greater than range start.			[if singleRange]
  Order of fields:
      Ascending order.			
      Descending order.			[error][if multipleRangeAndIndex]
      No order.			[error][if multipleRangeAndIndex]
			
#Delimitter
  Number of specified delimtters:
      None.			[property defaultDelimitter]			
      One.						
      More than one.			[error]
  Type of delimitter:
      Upper case letter.			[single][if !defaultDelimitter]
      Lower case letter.			[single][if !defaultDelimitter]
      Positive Integer.			[single][if !defaultDelimitter]
      Punctuation mark.			[if !defaultDelimitter]
      Space.			[if !defaultDelimitter]
      Tab.			[if defaultDelimitter]
      Special character.			[if !defaultDelimitter]
  
# Filename
  Presence of a file corresponding to the name:
      Not present.			[error]	
      Present.
